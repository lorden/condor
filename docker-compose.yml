version: '3.8'

services:
  backend:
    build: ./backend
    container_name: condor-backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      DATABASE_PATH: /app/data/condor.db
