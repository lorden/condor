{
  "name": "condor-extension",
  "version": "1.1.2",
  "description": "Chrome new tab extension for bookmark management",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/sync-version.js",
    "build": "vite build",
    "version": "node scripts/sync-version.js && git add manifest.example.json"
  },
  "dependencies": {
    "@carbon/icons-vue": "^10.99.1",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "vite": "^4.0.0"
  }
}
